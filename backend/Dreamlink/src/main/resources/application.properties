spring.application.name=Dreamlink
spring.profiles.active=${SPRING_PROFILES_ACTIVE:local}

spring.main.banner-mode=off
logging.level.root=WARN
logging.level.org.springframework.security=DEBUG
logging.level.com.example.dreamlink=DEBUG

# --- PostgreSQL
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/dreamlink}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:06!?.,Admin,.?!06}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true

# --- Sunucu Portu
server.port=${SERVER_PORT:8081}

# --- Python AI Matcher
ai.matcher.base-url=${AI_MATCHER_BASE_URL:http://localhost:8000}