{
  "permissions": {
    "allow": [
      "Edit",
      "Write",
      "Bash(*)"
    ],
    "deny": [
      "Bash(rm -rf *)",
      "Bash(git push *)",
      "Bash(git push)",
      "Bash(git reset --hard *)"
    ]
  },
  "hooks": {
    "Notification": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "powershell -Command \"[console]::beep(880, 400)\""
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "python -c \"import sys,json,os; d=json.loads(os.environ.get('CLAUDE_TOOL_INPUT','{}')); fp=d.get('file_path',d.get('path','')); [sys.stderr.write('BLOCKED: '+p+' is protected\n') or sys.exit(2) for p in ['.env','application-prod.properties','application-staging.properties'] if p in fp]\""
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "powershell -Command \"Write-Host '=== SESSION COST: Run /cost to check token usage ===' -ForegroundColor Yellow\""
          }
        ]
      }
    ]
  },
  "env": {
    "CLAUDE_AUTOCOMPACT_PCT_OVERRIDE": "75"
  }
}